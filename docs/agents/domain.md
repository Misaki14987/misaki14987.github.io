# Domain docs

This is a single-context repository.

Before exploring the codebase, read:

- `CONTEXT.md` at the repository root
- Relevant architectural decisions under `docs/adr/`

If these files do not exist, proceed silently. Domain-modeling skills create them only when project terminology or architectural decisions are resolved.

Use terminology defined in `CONTEXT.md` consistently. If work contradicts an existing ADR, identify the conflict explicitly rather than silently overriding it.
